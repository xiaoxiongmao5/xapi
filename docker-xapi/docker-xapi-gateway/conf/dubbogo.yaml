dubbo:
  registries:
    nacos:
      protocol: nacos
      # address: nacos:8848
      address: 172.19.17.153:8848
  consumer:
    references:
      UserIntefaceInfoClientImpl:
        protocol: tri
        interface: "" # 设置为对应的接口名称
      IntefaceInfoClientImpl:
        protocol: tri
        interface: "" # 设置为对应的接口名称
      UserInfoClientImpl:
        protocol: tri
        interface: "" # 设置为对应的接口名称
